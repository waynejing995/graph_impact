# Gap Inventory Before Code

Date: 2026-05-17
Status: Blocking inventory before more product code

## Purpose

This file is the first-stop checklist before any more implementation work. It is a rolling status inventory, not a frozen historical baseline. It lists every current gap document, the truth status, and the evidence that must exist before the active goal can be called complete.

For the complete product-promise register and user-visible complaint mapping,
read [Gap Document Register Before Code](2026-05-17-gap-document-register.md)
first. This inventory is the compact checklist; the register is the execution
map.

Rule for the next implementation pass: pick one gap ID, write the failing test first, make the smallest code change to close that test, then update that same gap document with verification evidence.

## Current Count

- Gap documents listed here: 22 Markdown files in `docs/gaps`.
- Product gap IDs: G01-G17, plus the AQ support matrix.
- Closed gaps: 0.
- Current pass verified but final G11/git gate remains: G01, G03, G04, G05, G08, G10, G12, G14, G16, G17 slices touched by the latest graph/UI/backend work.
- Partial or explicitly deferred boundaries: G02, G06, G09, G13, G15, AQ, plus the explicit G03 full clangd/libclang residual. G07 real MCP runtime smoke is now verified in the bundled Python 3.12 runtime, and G06 now has full local temp-copy provider embedding coverage plus query-time provider-vector rerank wiring.
- Blocking gate: G11 final documentation, artifact hygiene, commit, push, and explicit residual-boundary acceptance.
- 2026-05-17 user-review update: `/graph` must use a maintained React/npm graph package, standard UI must prefer shadcn/native package primitives, and Acceptance fail/partial rows must expand into real details.
- 2026-05-17 graph-architecture correction: graph build must be two-stage. Stage 1 is deterministic code graph extraction from source using an AST/preprocessor/macro-expansion style path where available. Stage 2 is LLM semantic-edge overlay on top of indexed code/doc/register candidates. Evidence/query-term overlay is explicitly separate and cannot be counted as Stage 1.
- 2026-05-18 vtable correction: full clangd/libclang vtable type-flow parsing is not done. Current callback/common links come from source-span table initializer, bounded returned-table alias facts, and slot-call matching plus shallow receiver type hints. The typed callback precision slice adds selective `clang -Xclang -ast-dump=json` receiver-type provenance (`type_flow=clang_ast_json`) and now makes that typed receiver override generic `funcs` leaf-name fallback; it also resolves callback table initializers that point to uniquely known functions defined in another file. The AST JSON callback-initializer slice adds `callback_initializer_flow=clang_ast_json` for macro-wrapped initializers and records `6` such rows after the latest live rebuild. The returned-table alias slice proves `select_gfx_funcs() -> gfx_v11_0_ip_funcs` constrains `funcs->hw_init()` without overlinking same-type SDMA callbacks. Latest default graph rebuild records `2,248` typed callback rows, `44` cross-file callback rows, and `21` exact table-alias callback rows, but `source=clang_callback` still must not be read as full clangd output. The next proof must either add a true typed clangd/libclang cross-TU extractor or keep this as an explicit residual, while query graphs show `common -> callback -> register` backbones and cross-repo graph connectivity through shared normalized register nodes.
- 2026-05-18 query performance correction: query-scoped graph expansion now uses one multi-seed NetworkX build, empty/no-edge multi-seed paths return seed nodes instead of raising or rebuilding, and callable-symbol metadata checks avoid regex compilation hot paths. Six real dirty-DB queries and AQ01 Web acceptance timing are recorded in `docs/qa/2026-05-18-query-graph-performance-qa.md`.
- 2026-05-17 graph-budget and resolver-node correction: graph/retrieval/semantic budgets must come from `configs/workbench-limits.json` plus explicit user overrides, not hidden CLI/API/UI constants. Resolver wrapper/extractor names are operations/provenance, not graph entity nodes, so `WREG32`, `REG_SET_FIELD`, `SOC15_REG_OFFSET`, `amdgv_wreg32`, and `gpu_register` must not become mega-nodes through graph endpoints, selected seeds, query expected terms, stale evidence rows, or UI resolver tables.
- Support/index documents: README, gap register, compact inventory, final clean evidence package gate.

## Inventory

| ID | Document | Status | Next proof before closure |
| --- | --- | --- | --- |
| G01 | [Real Ingestion And Indexing](2026-05-16-g01-real-ingestion-indexing.md) | Historical clean-final pass; expanded default-DB gate open | The named clean-final artifact `/tmp/asip-clean-amd-gemma4-final-current-2026-05-18.db` records code/doc/pdf/register counts, successful clean jobs, browser/product-surface proof, and AQ01-AQ09 9/9. Current default `data/asip.db` was expanded on 2026-05-20 to include `1101` `linux-amdgpu` documents, including `476` register headers; G11 still owns final residual review, browser/provider proof, commit, and push. |
| G02 | [Live Retrieval And Evidence Schema](2026-05-16-g02-live-retrieval-evidence-schema.md) | Partial; quality boundary open | Live inspector linkage, clean AQ runner mechanics, source diversity, query-time provider-vector wiring, and provider query-embedding fallback metadata are proven. Production semantic ranking quality, cross-source ranking, and final route/visual evidence remain open. |
| G03 | [Dynamic Weighted Graph](2026-05-16-g03-dynamic-weighted-graph.md) | Partial; clangd/type-flow residual | Fresh real AMD rebuild and 2K browser QA now prove a real Stage 1 deterministic graph plus small Stage 2 `gemma4:e4b` overlay. This pass adds shared graph-budget config, compile_commands-driven macro preprocessing, conservative direct/callback callgraph edges, cross-file unique direct common-helper calls, cross-file callback initializer calls, generic common `vtable_dispatch` candidate edges, receiver type-hint filtering, local/field-path callback table aliases, bounded returned-table aliases with ambiguous selector rejection, typed receiver precedence over generic `funcs` fallback, IP-block version/function-table aliases, corpus-level IP-block registration aliases, selective Clang AST JSON receiver-type hints, selective Clang AST JSON macro-wrapped callback-initializer hints, MxGPU `amdgv_init_func` dispatch coverage, query-scoped multi-seed performance fixes, exact function-node graph fallback, resolver-operator mega-node rejection, Markdown/PDF section endpoint proof, filtered semantic endpoints, and BoxMatrix-style LLM doc boxes. Older raw/debug QA recorded `doc_box/doc_section` document subtype shapes; the 2026-05-19 product projection records default output as `function/register/doc` with document subtypes folded into `attr.doc_kind`. Remaining proof is full Linux-kernel compile DB/clangd-quality cross-TU type-flow coverage, larger robust semantic batches, ranking review, and final design-doc closure. |
| G04 | [Corpus Management](2026-05-16-g04-corpus-management.md) | Current clean corpus flow verified; orchestration boundary | Selected UI indexing, invalid-source/zero-doc false-success prevention, durable `queued/indexing/succeeded/failed` job events, a clean named DB BFF add-index-query graph proof, and a real Web add-index-query graph/inspector loop are proven. Remaining boundary is non-MVP orchestration such as background workers, cancellation, remote clone orchestration, and arbitrary UI DB selection. |
| G05 | [Resolver Profiles](2026-05-16-g05-resolver-profiles.md) | Current pass verified; final gate | YAML-backed listing, add/validate/enable-disable/edit, per-index selection, CLI/API/MCP/Web BFF passthrough, and changed extraction without resolver code edits are proven. QA is recorded in `docs/qa/2026-05-18-resolver-profile-selection-qa.md`. Remaining boundary is richer unmatched-span diagnostics and broader non-C strategies beyond configured Python call extractors. |
| G06 | [Provider Settings And Ollama Detection](2026-05-16-g06-provider-settings-ollama.md) | Partial; explicit provider-quality boundaries | Local Ollama settings now drive embeddings, query-time provider-vector rerank, query/batch semantic-edge jobs, bounded provider backfill, and full temp-copy provider embedding coverage with provenance. The clean/current provider artifacts use `gemma4:e4b` instead of stale qwen rows, safe env-based extra-header expansion is tested, 128 chunks backfilled through `nomic-embed-text:latest` in `17.703s`, full temp copy reached `21884 / 21884` provider embeddings in `2388.07s`, and a local Ollama throwaway DB returned `provider-vector` at query time; remaining proof is credentialed OpenAI-compatible live endpoint QA or explicit local-compatible deferral, plus production-scale semantic ranking quality. |
| G07 | [API And MCP Product Surfaces](2026-05-16-g07-api-mcp.md) | Deterministic and real MCP runtime pass verified | API/MCP expose corpus, index jobs, provider status, evidence/entity detail, deterministic structured resolved-chain explanations, resolver validation, acceptance execution, semantic-edge generation, key read-route no-mutation/status-list no-migration behavior, FastAPI live HTTP smoke including `pnpm dev:api`, MCP server tool-matrix registration, Web/MCP query/evidence/entity/graph agreement, and bundled-Python real MCP runtime smoke. |
| G08 | [PDF And Document Ingestion](2026-05-16-g08-pdf-document-ingestion.md) | Current pass verified; content-depth boundary | Reduced AMD amdgpu PDF is indexed in the clean AMD DB (`documents.pdf=1`, `evidence.pdf=5`) and AQ05 passes with `pdf`; Markdown/PDF raw section facts, semantic section endpoints, PDF section provenance, LLM doc boxes, and clean-final API/browser page-citation QA for `amdgpu-driver-source-tree.pdf#page-1` are covered. Current product graph output projects those document facts to `kind=doc` with `attr.doc_kind`. Richer real PDF content depth remains a boundary. |
| G09 | [SQLite FTS5 Vector And NetworkX Runtime](2026-05-16-g09-storage-vector-graph-runtime.md) | Partial; provider-quality boundary open | Native sqlite-vec extension smoke and native `search_vector()` adapter are verified in the bundled Python runtime; system Python keeps the documented JSON/Python cosine fallback path; query evidence exposes `vector_runtime`; query-time provider-vector wiring is proven; NetworkX graph runtime is proven in final QA. Remaining proof is current-DB full provider-vector coverage/semantic rerank quality, not native adapter wiring. |
| G10 | [Testing Acceptance And Visual QA](2026-05-16-g10-testing-acceptance-visual-qa.md) | Current expanded-DB gate blocking | Historical clean/browser QA remains useful, but the 2026-05-20 expanded `data/asip.db` artifact is `0 passed / 8 partial / 1 failed`: DB/schema/source diversity pass, AQ09 semantic-edge rows exist but provenance freshness is `partial/stale` (`14` stale edges from job `4`, latest index job `10`), and live provider smoke plus fresh browser/e2e remain blocked. The supplemental Web-included run is `0 passed / 0 partial / 9 failed` because CLI/API/MCP probes return rows while every Web probe is `not_configured` without `ASIP_WEB_BASE_URL`. |
| G11 | [Completion Gate And Documentation Review](2026-05-16-g11-completion-gate.md) | Blocking | Design docs, gap docs, QA docs, git diff, tests, commit, and push are reviewed in one final completion gate. |
| G12 | [ASIC And IP Metadata Filtering](2026-05-16-g12-asic-ip-metadata-filtering.md) | Current real AMD filter pass verified; heuristic boundary | Core/Web BFF/UI plus FastAPI and MCP filter tests prove IP/ASIC filters affect result sets; clean-final API/browser QA proves representative real AMD filtered queries; path/symbol heuristic inference limits remain documented as the MVP boundary. |
| G13 | [MVP Boundary And Full-Spec Deferrals](2026-05-16-g13-mvp-boundary-deferrals.md) | Blocking | Full-spec non-MVP features are mapped to explicit deferrals instead of silent omissions. |
| G14 | [Static Data And Truthful Empty States](2026-05-16-g14-static-data-and-truthful-empty-states.md) | Current pass verified; audit residual | Empty/error/no-mutation paths, source filters, live global search, package graph, and expandable acceptance details are covered; remaining work is final route audit consistency in G11. |
| G15 | [Performance Smoke And Deterministic Rebuild](2026-05-16-g15-performance-smoke-deterministic-rebuild.md) | Current performance pass with explicit long-run boundaries | Fixture stable-count smoke is implemented and documented: two empty-DB rebuilds over `docs/fixtures/performance-smoke` match counts, and five live fixture queries return rows under one second. AMD query-graph performance QA records more than five real query timings and AQ01 Web acceptance under the 30s e2e timeout. Repeat mxgpu + linux-amdgpu deterministic graph rebuild records `131.639s` and `126.034s` on live DB backups with stable counts; bounded provider backfill embeds `128` chunks in `17.703s`; full local temp-copy provider backfill covers `21884 / 21884` chunks in `2388.07s`; empty-DB raw corpus re-index timing is recorded for the current selective raw path. Future all-file code indexing scale, hosted-provider throughput, and broader semantic ranking latency remain boundaries. |
| G16 | [Workbench IA Theme And Visual Fidelity](2026-05-16-g16-workbench-ia-theme-visual-fidelity.md) | Current pass verified; final gate | shadcn/Radix controls, package-backed graph rendering, live global search, source filters, semantic controls, light/dark persistence, and visual route QA are verified; recapture only if further UI changes land. |
| G17 | [Architecture Ownership And Process Shape](2026-05-16-g17-architecture-ownership-process-shape.md) | Current pass recorded; final review | Core/API/MCP/Web ownership now splits Stage 1 deterministic code graph including direct/callback/generic dispatch call edges, evidence overlay, Stage 2 LLM semantic-edge overlay, resolver boundaries, package graph adapter, and shadcn UI composition; full clangd/libclang type-flow callback resolution remains explicit future ownership, and final G11 review owns residual boundary acceptance. |
| AQ | [MVP Acceptance Query Matrix](2026-05-16-mvp-acceptance-query-matrix.md) | Blocking; current expanded-DB and Web-included artifacts recorded | Historical clean AMD gemma artifacts record AQ01-AQ09 9/9. The current expanded default-DB artifact records schema pass and AQ05 source diversity, but summary `0 passed / 8 partial / 1 failed`; the supplemental Web-included artifact records `0 passed / 0 partial / 9 failed` with Web `not_configured`. Final G11 closure still needs browser/provider proof, stale-doc reconciliation, and git gate. |

## Support Gates

| Document | Role | Required use |
| --- | --- | --- |
| [Final Clean Evidence Package Gate](2026-05-17-final-clean-evidence-package.md) | Defines the final QA package required before completion. | Every G01-G17 closure claim must link to this package or explicitly explain a user-accepted deferral. |

## Audit Findings To Preserve

- P0: The historical clean provider AQ artifact proves provider plumbing, not cross-source retrieval. Current source-gated rerun is 0/9 and correctly exposes DB health failures plus missing PDF on AQ05.
- P0: G01 and G08 now have clean DB counts, PDF evidence, clean-final UI/API/browser citation QA, and final completion-gate review still pending.
- P0: The multisource fixture artifact proves the source-diversity path on a synthetic corpus only; it must not be promoted to real AMD closure.
- P0: G16 now has fresh visual QA; it remains blocked only on final design/spec review and any future UI-affecting changes requiring recapture.
- P1: G06/AQ09 still needs credentialed OpenAI-compatible live endpoint QA or an explicit user-approved local-compatible boundary; safe env-based header expansion, bounded Ollama provider backfill, and full local temp-copy provider embedding coverage are implemented and tested.
- P1: G09 native sqlite-vec extension smoke and native `search_vector()` adapter path are verified in the bundled Python runtime; remaining G09 risk is full provider-vector coverage and semantic rerank quality, not adapter wiring.
- P1: G15 now has fixture stable-count rebuild, sub-second query evidence, repeat real-corpus deterministic graph rebuild timing, bounded provider backfill timing, full local temp-copy provider coverage timing, and empty-DB raw re-index timing for the current selective path; future all-file code indexing remains a separate scale boundary if kept in scope.
- P1: G07 real MCP runtime smoke is now verified in bundled Python 3.12; G14 still needs remaining final fallback/demo-state audit in G11.
- P0: `/graph` live API data is not enough; Stage 1 deterministic code graph extraction and Stage 2 semantic-edge overlay must be separate, visible in provenance, and visually/interaction tested as a real global graph with function-operation, direct/callback call, document-section, and semantic-edge layers. Resolver macro/wrapper names and callback slot names must remain provenance only, never central graph nodes.
- P0: Acceptance rows must expose fail/partial detail; compressed counts alone are not usable QA.
- P1: Standard frontend controls should use shadcn/native package primitives before custom markup.

## No-Code Pause Checklist

- Product code is paused until this inventory and the individual gap docs are the planning source of truth.
- Docs-only edits may continue to correct stale facts or clarify acceptance evidence.
- The next code task must name the gap ID it closes and must start with a failing test.
- No commit, push, or goal completion claim is allowed while any row above is still blocking.

## Immediate Next Workstream Candidates

1. G03/G06/G08/G16/G17: harden the proven two-stage graph path: full compile-command/libclang-quality extraction, robust larger `gemma4:e4b` semantic batches, doc/PDF semantic coverage, ranking review, and design-doc closure.
2. G14/G10/G16: remove/demote remaining static product data and add expandable acceptance detail UI.
3. G04/G05/G06/G07: finish or explicitly bound corpus, resolver profile, provider, and OpenAI-compatible secret handling.
4. G02/G03/AQ: reconcile clean AMD 9/9 and free-form query records with browser graph/inspector evidence.
5. G01/G08/G09/G12/G15: finish remaining clean AMD UI/API citation, vector/runtime boundary, semantic ranking review, and performance proof beyond the current counts/timings.
6. G16/G10/G11/G17: rerun visual QA, finish architecture/design/documentation review, git hygiene, commit, and push.
