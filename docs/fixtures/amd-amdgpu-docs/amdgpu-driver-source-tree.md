# Reduced AMD amdgpu Documentation Fixture

Source: https://instinct.docs.amd.com/projects/amdgpu-docs/en/latest/

This reduced fixture paraphrases the public AMD GPU Driver (amdgpu) documentation for ASIP ingestion QA. The official AMD documentation describes amdgpu as the AMD GPU driver used with ROCm, virtualization, and data center GPU software stacks.

For ASIP evidence linking, the corresponding Linux kernel driver source tree is represented by drivers/gpu/drm/amd/amdgpu. This document intentionally includes the terms amdgpu documentation, AMD GPU driver, and driver source tree so the clean acceptance query can retrieve doc evidence alongside code and PDF evidence.

Key ASIP QA terms: amdgpu documentation, AMD GPU driver, driver source tree, Linux source tree, drivers/gpu/drm/amd/amdgpu, ROCm, virtualization.
