# Performance Smoke Fixture

`GCVM_L2_CNTL` controls the L2 cache path in this fixture.

`IH_RB_CNTL` configures the interrupt ring buffer.

`SDMA0_QUEUE0_RB_CNTL` documents SDMA queue setup.

`CP_INT_CNTL_RING0` is included so query timing covers another register family.
