# Raw Imagegen Outputs

These files are the direct imagegen outputs for each page prompt. They are preserved as generation evidence and design inspiration.

Do not use this directory for visual QA pass/fail. Raw imagegen outputs can drift in top bar height, sidebar width, and content origin. Use `../images/` for canonical normalized anchors because those images are captured from the live app at the shared 2048 x 1280 baseline.
